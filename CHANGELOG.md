# Changelog

## 0.1.0 — unreleased

First version. Three roles (`developer`, `reviewer`, `integrator`), the `wtc`
command (`init`, start, `attach`, `status`), vendor-neutral by design with one
verified tool integration (Claude Code) and one unverified (Codex), five
subagents from agency-agents, four project examples.
