# Role: none

**Do not work in this directory.**

It carries a `.agents/ROLE` of `none` on purpose: it is a checkout that exists
for some other reason — an old clone, a scratch area, a restore test — and
anything built here will be confusing at best and lost at worst.

Ask the human which worktree to use instead.
